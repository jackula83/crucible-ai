---
topic: Crucible — semantic assertion library for stateful systems
updated: 2026-08-11T18:52
---

- (event) Workspace bound; inputs: README.md (concept + code sample + competitor analysis promptfoo/DeepEval). uv missing, memlog runs via python3. Landscape research subagent dispatched.
- (decision) Stakes: OSS launch (public npm library, README-driven adoption) — full rigor. Working mode: Coaching path.
- (decision) Coaching entry point: Vision + Features (capability-first). Form-factor: TypeScript library, in-process, inside existing test frameworks (Jest/Vitest).
- (event) Landscape research digest saved (research-landscape.md). Key: whitespace confirmed — no incumbent does world-state-first + in-process SUT + pass^k thresholds in TS; semantic-expect = probabilistic-assertion prior art to cite; watch vitest-evals/evalite/DeepEval-TS as fast-follow threats.
- (decision) Vision: code-first semantic test library for non-deterministic inference outputs; cross-platform ambition — Jest first, then xUnit, JUnit, others. Audience: anyone testing stateful OR non-stateful LLM systems who prefers code-first. Domains: AI storytellers, book-writing assistants, roleplay, AI NPCs (games), chatbots. Framework domain-agnostic; user floats schema backend + agent-followable instructions.
- (decision) Positioning: code-first = state mutable inside unit of work (dynamic arrange), vs promptfoo front-loaded prep; stateless = degenerate case (no state arrange). Superset story, stateful-first differentiator.
- (decision) v1 scope: TS/Jest MVP. Terminology: language-agnostic (not platform-agnostic). Architecture split: deterministic shell (N runs + threshold/confidence check) = language-specific, ported per language later (Python/.NET/Java); non-deterministic assertion semantics = language-agnostic schema, markdown rule files an AI agent (Claude) follows to execute the test; function calls map to instruction sets.
- (override) User correction: 'world state' is Proscenium-specific vocabulary — framework must be domain-agnostic. Docs/PRD must use a generic term a random dev groks instantly; Proscenium world-log = one example only, not the target implementation.
- (change) Scrubbed 'world state/world-log' from research-landscape.md and addendum.md per terminology rule; proper nouns (WorldMemArena) retained.
- (decision) Generic term: 'state' (context acceptable); model = arrange state before act. MVP assertion: coherence() only; future candidates range(), exact() — open list, not exhaustive.
- (decision) Judge config: crucible.config.json at project root — provider (MVP: OpenRouter), model (e.g. deepseek-v3), meta = provider-specific passthrough params (OpenRouter: provider/reasoning; Bedrock: others); per-provider docs required at GA. CI: GitHub Actions. User funds OpenRouter account for e2e testing.
- (decision) Failure UX: default shows first judge error per failing run; config-level verbosity option for full log output; no file dumping — user redirects stdout themselves (yarn test > output.log). Env-var override (CRUCIBLE_VERBOSE) proposed since Jest has no native flag passthrough — pending confirm. Judge false-negatives = accepted limitation; mitigated because judge makes narrow single assertions against provided state, not full-state interpretation; frontier models expected reliable.
- (decision) Verbosity: config file default + env var override (env wins). Runs semantics settled: repetition at TEST level, not assertion — inferenceIt('test', {runs, threshold}, body) re-runs whole arrange/act/assert body N times (avoids state-mutation-after-input problem); assertion inside stays single-shot toMeet(coherence(...)). README code sample now stale — user asked to update README at end of session.
- (event) Pending task: update README.md code sample + narrative to inferenceIt semantics at session end.
- (decision) Assertion surface: native framework assertions, no custom matchers — crucible.coherent(response, claim) returns boolean (async); expect(...).toBe(true) / Assert.True / assertTrue per language. Crucible engine prints judge diagnostics on failure (verbosity engine owns output, not the matcher). toMeet() dropped. runs optional (default 1 = smoke mode). Parallel runs by default (SUT isolated per arrange); configurable post-MVP.
- (decision) State API: strings, KISS — crucible.load()/append accept string(s); users may pass JSON-as-string; post-MVP idea: user-written custom parsing instructions for the judge agent. Multi-step arrange/act/assert cycles: not prevented (appending state works), AAA single-cycle recommended as best practice; no special support built.
- (decision) Success metrics: user indifferent ('anything is good') — PRD carries lightweight adoption signals only, tagged assumption. Scope addition: observability IS in MVP — debug log level dumping maximal detail during runs (engine init, executions, provider connection). FG5 becomes Diagnostics & Observability with log-level ladder. Feature groups FG1-FG6 confirmed (no redlines).
- (event) Full PRD draft assembled: 6 feature groups, FR-1..FR-9, 6 open questions, 5 indexed assumptions. Dev-product clusters pulled in (public surface via FRs, Why Now); UJs kept to one-liners per library scope dial.
- (decision) API keys via environment variable only; crucible.config.json stays committable. OQ-1 narrowed to naming + CI docs. Finalize started.
- (event) Memlog audit walked with user — confirmed, no orphaned decisions.
- (change) Reviewer gate resolved: 4 criticals fixed in PRD — (C-1) per-run state isolation contract added to FR-3, mechanism deferred to OQ-7; (C-2) judge infra errors = errored runs, never semantic failures (FR-7), retry policy folded into OQ-3; (C-3) unasserted verdicts = user responsibility like plain Jest, documented gotcha; (C-4) integer-safe pass rule ceil(threshold*runs) + binomial headroom docs requirement in FR-1 notes. H-1 threshold scale fixed to [0,1] in glossary + addendum stale API example updated. Rubric med/low: FR-4 consequence added, FR-8 consequences + excerpt cap assumption, judge cost note.
- (change) README reconciliation applied: (G-2) anti-omniscience/knowledge-boundary claims restored as first-class FR-5 consequence; (G-3) promptfoo framing softened to README's fairer voice; (G-4) SUT-harvested state (story.getLogs()) added to UJ-1 + FR-3. (G-1) title 'non-deterministic systems' kept — deliberate superset positioning per earlier decision. (G-5) origin-story hook ignored for PRD, lives in README.
- (event) Triage: OQ-2 fact-checked — npm 'crucible' taken, 'crucible-ai'/'crucible-test' free; name choice deferred to user (owner: Jack, revisit: before publish). OQ-1/3/5/6/7 = architecture-phase, non-blocking. Assumptions accepted as tagged. No phase-blockers — PRD safe for architecture/epics.
- (decision) Package name decided: crucible-ai (npm-verified free). OQ-2 resolved.
- (event) PRD finalized
- (change) Post-finalize README edit per user: Alternatives section reworded out of second person ('world log'→'state' applied too) + added research top picks: vitest-evals/evalite (closest TS neighbours), semantic-expect (prior-art credit), Braintrust/LangSmith (platform plays).
- (decision) Update intent post-finalize: runner renamed inferenceIt → crucible.it() (whole surface on one crucible namespace). Per-language binding sketches added to addendum + PRD 4.6 + README: xUnit [CrucibleFact(Runs, Threshold)] via FactAttribute extension; JUnit 5 @CrucibleTest via TestTemplateInvocationContextProvider; pytest @crucible.it decorator. Applied across prd.md, addendum.md, README.md.
- (change) README: added collapsible (details/summary) C#/xUnit, Java/JUnit5, Python/pytest example ports under TS sample, labelled roadmap (v1 = TS/Jest). GFM has no native tabs — details blocks used instead.
- (change) Post-finalize patch (architecture run 2026-08-10): §6.1 CI line updated — CI = lint/typecheck/unit only, e2e local-only (e2e/core pre-commit + Makefile) per spine AD-12; supersedes 'e2e in CI'.
- (change) §6.1 CI line re-patched: main-merge lane runs e2e/core vs funded OpenRouter; local e2e manual (no pre-commit) per amended AD-12.
